#include "../../../include/header/types/state/stateMatrix.h"

/* CONSTRUCTORS */
//StateMatrix::StateMatrix() : DataMatrix() {}

//StateMatrix::StateMatrix(VECTOR_SIZE_TYPE nr, VECTOR_SIZE_TYPE nc) : DataMatrix(nr, nc) {}

//StateMatrix::StateMatrix(Eigen::Matrix<SCALAR, Eigen::Dynamic, Eigen::Dynamic> mat) : DataMatrix(mat) {}

//StateMatrix::StateMatrix(const StateMatrix & mat) : DataMatrix(mat) {}

/* DESTRUCTOR */
//StateMatrix::~StateMatrix() {}

/*StateVector * StateMatrix::row(VECTOR_SIZE_TYPE i) const {
    assert(i < this->nrows_); 
    return new StateVector(this->data_.row(i));
}

StateVector * StateMatrix::col(VECTOR_SIZE_TYPE j) const {
    assert(j < this->ncols_); 
    return new StateVector(this->data_.col(j));
}*/
