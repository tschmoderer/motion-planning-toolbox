name: docs

on:
  push:
    branches:
      - main
    paths: 
      - '**.cpp'
      - '**.hpp'
      - '**.c'
      - '**.h'
  workflow_dispatch:

jobs:
  docs:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Doxygenize
        uses: langroodi/doxygenize@v1.4
        with: 
          doxygenconf: './docs/doxyconfig.in' # default is ./Doxyfile
          htmloutput: './docs/html/'          # default is ./docs/
          ghpagesbranch: 'gh-pages'               # default is gh-pages
          ghpagesdir: './docs/html/'           # default is ./ 
